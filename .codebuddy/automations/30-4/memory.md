# 占卜爱好者体验检查 - 执行历史

> 环境基线更新（2026-03-18）：当前本地标准环境已切换为 **phpstudy + `http://localhost:8080` 直连接口**。后续占卜巡检不要再默认使用 Docker、容器日志、`docker compose` 或 3001 代理；历史记录中的相关内容仅作旧证据，不再作为当前执行前提。


## 2026-03-17 第十九轮执行记录

### 检查概览
- **检查时间**: 2026-03-17
- **检查人员**: 资深占卜爱好者（AI专家审计）
- **检查范围**: 深度审计八字、六爻、塔罗、合婚、日运之核心逻辑与算法精度。
- **核心发现**: 
    1. **八字**: 发现 `getJieQiDate` 仅适配21世纪，导致20世纪出生者节气计算存在 1天左右误差。
    2. **合婚**: 命卦计算忽略立春，导致公历年初出生者命卦完全错误。
    3. **六爻**: 缺失“伏神”系统，无法满足专业占卜需求。
    4. **塔罗**: 元素互动描述过于“中式五行化”，且部分牌组逆位释义深度不足。
    5. **日运**: 发现日柱计算与主排盘服务算法不一致的风险点。

### 处理结果
已将发现的 3 项高优先级逻辑错误、3 项中优先级体验问题、2 项低优先级专业性优化建议记录至 `TODO.md` 的“🔮 占卜爱好者深度体验检查报告 - 第十八轮”部分。
建议优先修复 20世纪节气常数缺失及合婚命卦立春判定问题。

## 2026-03-17 第二十轮执行记录

### 检查概览
- **检查时间**: 2026-03-17
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: 八字、六爻、塔罗、合婚、每日运势的页面体验 + 接口/代码交叉核验。
- **核心发现**:
  1. **八字**：`getLunarMonth()` 节气定月逻辑倒序，测试生辰月柱被错排为丑月。
  2. **六爻**：前后端接口命名失配，`getPricing/divination` 方法不存在，页面无法真正起卦。
  3. **合婚**：免费预览直接 500，`Hehun.php` 读取不存在的 `zhi_index`。
  4. **日运**：前端字段映射与后端返回结构不一致，同时黄历年号出现明显错误。
  5. **塔罗**：单张/三张/凯尔特十字可出牌，但凯尔特十字关系分析仍偏模板化。

### 处理结果
已将本轮新增的 6 项高优先级、3 项中优先级、2 项低优先级问题写入 `TODO.md` 的“🔮 占卜爱好者深度体验检查报告 - 第二十轮”部分。
建议优先修复：八字节气定月、六爻路由失配、合婚 `zhi_index` 崩溃、每日运势前后端字段错位。

## 2026-03-17 第二十一轮执行记录

### 检查概览
- **检查时间**: 2026-03-17
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: 前台真实登录体验 + 占卜接口交叉核验，重点复查八字页可用性、八字底盘一致性、每日运势个性化底盘与塔罗专业表达。
- **核心发现**:
  1. **八字**：已登录访问 `/bazi` 出现白屏；simple 结果仍把测试生辰错排为 `己丑` 月，且五行统计全 0。
  2. **日运**：`todayGanZhi` 仍算成 `庚戌`，签到状态接口还因 `checkin_record` 缺表在首屏弹错。
  3. **塔罗**：凯尔特十字已分牌位，但障碍位仍直接套原牌义，元素互动段夹杂英文术语。

### 处理结果
已将本轮新增的 4 项高优先级、1 项中优先级、1 项低优先级问题写入 `TODO.md` 的“🔮 占卜爱好者深度体验检查报告 - 第二十二轮”部分。
建议优先修复：八字页白屏、simple 月柱/五行统计矛盾、每日运势个性化干支错误、签到状态表名错位。

## 2026-03-17 第二十三轮执行记录

### 检查概览
- **检查时间**: 2026-03-17
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: 前台真实页面体验 + 接口/代码交叉核验，重点复查登录入口、八字页可用性、合婚主链路、塔罗积分展示与每日运势个性化底盘。
- **核心发现**:
  1. **登录**：测试验证码登录仍因 `SmsService` 缺少 `Log` 引用而 500。
  2. **八字**：`/bazi` 路由主体未渲染排盘表单，页面内容仍错位到首页文案。
  3. **合婚**：`pricing/calculate` 都因 `calculatePointsCost()` 传入空积分配置而 500。
  4. **塔罗**：页面把当前积分误显示为 0，但积分接口实际仍有余额。
  5. **日运**：个性化今日干支仍算错，签到状态接口继续依赖不存在的 `checkin_record` 表。
  6. **六爻**：`divination` 方法不存在问题仍复现，但 `TODO.md` 已有同类记录，本轮未重复登记。

### 处理结果
已将本轮新增的 5 项高优先级、1 项中优先级、2 项低优先级问题写入 `TODO.md` 的“🔮 占卜爱好者深度体验检查报告 - 第二十三轮”部分。
建议优先修复：登录验证码 500、八字页内容错位、合婚积分配置空值、塔罗积分展示错位、每日运势个性化干支错误。

## 2026-03-18 第二十四轮执行记录

### 检查概览
- **检查时间**: 2026-03-18
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: 八字、六爻、塔罗、合婚、每日运势的运行态接口结果与现有代码实现交叉复核。
- **核心发现**:
  1. **八字**：流年分析的 `fortune_analysis.years[*].year` 仍使用数组索引，返回 `0~4` 而不是真实年份。
  2. **六爻**：运行环境里的 `pricing/divination` 仍报 `Liuyao->getPricing()/divination()` 方法不存在，实际无法起卦。
  3. **合婚**：premium 详细报告继续因 `male_birth` 字段缺失而失败，免费预览与付费链路状态不一致。
  4. **日运**：黄历主字段大量为空，但个性化仍单独返回 `todayGanZhi=辛卯`，展示底盘不一致。
  5. **塔罗**：解读文案仍有明显模板拼接与重复标点，专业感不足。

### 处理结果
已将本轮新增的 4 项高优先级、1 项中优先级、1 项低优先级问题写入 `TODO.md` 的“🔮 占卜爱好者深度体验检查报告 - 第二十四轮”部分。
建议优先修复：八字流年年份索引、六爻运行态方法不存在、合婚 premium 写库字段错位、每日运势黄历主数据缺失。

## 2026-03-18 第二十五轮执行记录

### 检查概览
- **检查时间**: 2026-03-18
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: 八字、六爻、塔罗、合婚、每日运势的真实接口复测 + 浏览器匿名访问 + 容器日志交叉核验。
- **核心发现**:
  1. **八字**：主排盘能出四柱，但 `yearly/dayun/chart` 三个深度接口全部 500，根因是 `Fortune.php` 错误实例化 `Paipan` 且 `YearlyFortuneService` 误写 `Db` 命名空间。
  2. **六爻**：运行环境不再是“方法不存在”，而是控制器硬编码 `tc_liuyao_record`，与库里实际 `liuyao_records/liuyao_gua` 表名不一致，定价与起卦链路一起 500。
  3. **塔罗**：single / three / celtic 抽牌均可返回牌面与正逆位，本轮未发现比既有 TODO 更高价值的新问题。
  4. **合婚**：pricing、free、premium（足额积分用户）链路均已恢复，本轮未新增合婚问题。
  5. **日运**：`/daily` 虽在前端声明为公开页，但匿名访问 `/api/daily/fortune` 直接 401，页面会被拦回首页。
  6. **登录入口**：测试模式下 `send-code` 仍报“短信服务未配置”，真实页面入口依旧阻塞需登录的占卜功能。

### 处理结果
已将本轮新增的 4 项高优先级、1 项低优先级问题写入 `TODO.md` 的“🔮 占卜爱好者深度体验检查报告 - 第二十五轮”部分。
建议优先修复：八字流年/大运深度接口 500、六爻表名错位、每日运势公开页鉴权冲突、测试模式验证码发送未走本地短路。

## 2026-03-18 第二十六轮执行记录

### 检查概览
- **检查时间**: 2026-03-18
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: 合婚剩余链路与每日运势登录态个性化复测，重点核验“未知时辰合婚”“详细报告结论一致性”“已登录用户专属运势是否真正生效”。
- **核心发现**:
  1. **合婚**：按前端真实 JSON 请求复测时，free / premium 链路都能返回结果，但 premium 报告内部出现严重冲突：总评仍给出 `81 分 / 佳偶天成`，同时 `traditional_methods.jiugong` 又判定为“五鬼”“大凶”“强烈建议慎重考虑”。
  2. **合婚**：付费勾选“AI 深度分析”后，返回体里的 `ai_analysis.is_ai_generated=false`，且附带“当前使用规则引擎分析”的 note；前端结果区仍以“AI深度解读”名义展示，没有显式告知已降级。
  3. **日运**：登录用户即使 `points/balance` 已显示 `baziCount=1`，且再次成功排盘后变成 `baziCount=2`，`GET /api/daily/fortune` 仍持续返回 `personalized=null`，专属运势卡实际永远不出现。
  4. **日运根因**：`Daily::fortune()` 依赖 `$request->user` 生成个性化结果，但当前控制器只给 `luck/checkin/checkinStatus` 挂了 Auth 中间件，`fortune` 公共路由不会注入登录用户，因此登录态和游客看到的是同一份非个性化结果。

### 处理结果
已将本轮新增的 2 项高优先级、1 项中优先级问题写入 `TODO.md` 的“2026-03-18 占卜深度体验补充（第四十七次）”部分。
建议优先修复：每日运势登录态个性化完全失效、合婚详细报告总评与九宫凶象相互矛盾、合婚 AI 降级未披露。

## 2026-03-18 第二十七轮执行记录

### 检查概览
- **检查时间**: 2026-03-18
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: 继续用真实用户态接口批量复测六爻、塔罗、合婚 premium 与每日运势，重点补齐上一轮未跑完的主链路，并与 `TODO.md` 做新增去重。
- **核心发现**:
  1. **日运**：测试账号 `13800138112` 在复测样例八字后，`points/balance` 已变为 `baziCount=2 / balance=90`，但 `GET /api/daily/fortune` 仍返回 `personalized=null`，确认该问题在当前运行态持续复现。
  2. **六爻**：`GET /api/liuyao/pricing` 已恢复，但 `POST /api/liuyao/divination` 的时间起卦、手动摇卦都只返回 `HTTP 200 + code 500`，`GET /api/liuyao/history` 仍为空，说明核心起卦链路尚未打通。
  3. **塔罗**：`single / three / celtic` 三种牌阵都能正常抽牌并扣分，但 `POST /api/tarot/interpret` 全部直接 HTTP 500；容器错误日志明确命中 `Class "app\\controller\\TarotElementService" not found`。
  4. **合婚**：使用新测试账号 `13800138113` 真实解锁 premium 后，详细报告仍给出 `81 分 / 佳偶天成`，同时九宫命卦继续判为“五鬼 / 大凶 / 强烈建议慎重考虑”，总评冲突未修复。

### 处理结果
已把本轮确认且尚未登记的 4 项高优先级问题补写到 `TODO.md` 的“2026-03-18 占卜深度体验补充（第四十七次）”部分。
建议优先修复：塔罗解读链路 500、六爻主链路 500、每日运势登录态个性化失效、合婚总评与九宫凶象冲突。

## 2026-03-18 第二十八轮执行记录

### 检查概览
- **检查时间**: 2026-03-18
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: 用真实 JWT 用户态接口重测八字、六爻、塔罗、合婚、每日运势，并补查积分流水，重点核验失败扣费、结果落库与登录前置链路。
- **核心发现**:
  1. **登录**：`/api/sms/send-code` 直接因缺少 `tc_sms_code` 表而 500，验证码登录入口再次阻断。
  2. **八字**：基础排盘本身可出正确四柱，但 `yearly` 与 `dayun-chart` 失败后仍各扣 30 分，属于失败扣费。
  3. **六爻**：时间/手动起卦都能返回卦象摘要，但 `id=null` 且历史为空，保存链路仍未打通；结果字段也缺少六亲六神等专业展示。
  4. **塔罗**：单张/三张/凯尔特十字都能抽牌，但 `interpret` 三次均失败，核心解牌主链路依旧中断。
  5. **合婚 / 日运**：本轮未发现比现有 TODO 更高价值的新阻塞；合婚总评与规则降级提示、每日运势登录态个性化均表现正常。

### 处理结果
已将本轮新增的 5 项高优先级、1 项中优先级、2 项低优先级问题写入 `TODO.md` 的“2026-03-18 占卜深度体验检查（第三十次）”部分。
建议优先修复：短信验证码缺表、八字深度分析失败扣费、六爻结果不落库、塔罗解读链路全量失败。

## 2026-03-18 第二十九轮执行记录

### 检查概览
- **检查时间**: 2026-03-18
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: 复查本地运行栈可用性，并结合代码/历史运行快照深挖每日运势核心算法与当前整站阻塞点。
- **核心发现**:
  1. **占卜通用**：本地执行 `start-local.ps1` 与 `docker compose up -d --build` 后，`taichu-app` 因 `20260318_fix_shensha_display_encoding.sql` 触发 `tc_shensha.uk_name_category` 唯一键冲突而持续重启，八字、六爻、塔罗、合婚、每日运势整站无法继续实测。
  2. **每日运势**：`DailyFortune::generateFortune()` 仍直接使用 `mt_rand()` 生成总分和四项分数，描述文案也独立随机拼接，存在“高分但结论偏平淡”的先天矛盾，专业可信度不足。

### 处理结果
已将本轮新增的 2 项高优先级、1 项中优先级、1 项低优先级问题写入 `TODO.md` 的“2026-03-18 占卜深度体验检查（第三十一次）”部分。
建议优先修复：先把神煞乱码修复 SQL 改成真正幂等，恢复后端启动；随后重做每日运势公共底盘，取消 `mt_rand()` 随机分数与随机文案拼接。

## 2026-03-18 第三十轮执行记录

### 检查概览
- **检查时间**: 2026-03-18
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: 从塔罗“历史/保存/分享”闭环继续往下补查，并复核当前本地运行态是否还能支撑真实用户链路。
- **核心发现**:
  1. **环境阻塞**：当前 `localhost:8080` 虽然 `GET /api/health` 仍返回 200，但端口实际由多进程 `nginx.exe` 占用；`POST /api/auth/phone-login` 等依赖数据库的接口已不再进入可用运行态，而是直接抛出 `php_network_getaddresses: getaddrinfo for mysql failed`。说明当前本机后端仍沿用 `DB_HOST=mysql`，但并未处在可解析该主机名的 Docker 网络里。
  2. **塔罗闭环**：基于上一轮已成功的 `draw + interpret` 结果继续回看代码与日志，`TarotRecord` 模型固定依赖 `spread_type/is_public/share_code/view_count`；而运行日志里先后出现 `Field 'type' doesn't have a default value` 与 `Unknown column 'is_public' in 'field list'`，高度指向 `tc_tarot_record` 仍存在旧表结构/脏结构漂移，保存、历史、公开分享链路不能稳定闭环。
  3. **六爻状态更新**：旧问题“`id = null` / history 全空”已不再成立；当前更准确的问题定义应改为“实时结果完整，但历史回读字段缩水”，根因仍在 `Liuyao::storeDivinationRecord()` 按表结构分支持久化。

### 处理结果
已把本轮确认的新环境阻塞与问题状态迁移写回 `TODO.md`：新增“本地 8080 运行态与数据库容器脱节”以及“塔罗 save/history/share 闭环疑似卡在旧表结构”两项高优问题，同时把已被前次实测证伪的“塔罗 interpret 全失败”“六爻 id=null/history 全空”移入已确认区。
本轮未继续做代码修复；后续若要恢复真实占卜链路，应先统一 8080 对应的后端运行方式（容器 / 本机二选一），再复打塔罗保存、公开分享与历史回看接口。

## 2026-03-18 第三十一轮执行记录

### 检查概览
- **检查时间**: 2026-03-18 21:32
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: phpstudy 基线下复测登录前置、每日运势公开链路，以及六爻 / 塔罗 / 合婚的登录后入口可用性。
- **核心发现**:
  1. `GET /api/health` 仍为 200，但 `POST /api/auth/phone-login` 稳定报 `SQLSTATE[HY000] [1045] Access denied for user 'taichu'@'localhost'`。
  2. `GET /api/daily/fortune` 在游客态与带 JWT 两种情况下都直接 500，说明公开日运也被当前 MySQL 凭据问题拖死。
  3. 带有效 JWT 复测时，`GET /api/hehun/pricing`、`GET /api/tarot/history` 报同一条 1045，`GET /api/liuyao/pricing` 则返回 `HTTP 200 + code 500`；当前无法继续进入扣费 / 历史 / 分享闭环。
  4. `http://localhost:5173/bazi` 与 `http://localhost/bazi` 都是连接拒绝，本轮没有可补测的前台页面实例。

### 处理结果
- 已把 `[15]` 数据库凭据阻塞项的影响范围补写到 `TODO.md`，明确其已扩散到每日运势公开链路与多条占卜入口/历史链路。
- 原始接口证据已落盘到产物目录 `divination_probe_30_4_output.json`，供后续接力时直接复用。
- 本轮未做代码修复；下一步仍应先确认 phpstudy 正在使用的 MySQL 用户名/密码，再重跑真实占卜闭环。

## 2026-03-19 第三十二轮执行记录

### 检查概览
- **检查时间**: 2026-03-19 01:45
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: phpstudy 基线下复测登录前置、每日运势公开链路，以及登录后占卜入口（合婚 / 六爻 / 塔罗）的可进入性；同步确认本地前台页面是否存在可直接访问实例。
- **核心发现**:
  1. `GET /api/health` 仍为 200，但 `POST /api/auth/phone-login` 继续在 `User::findByPhone()` 查询 `tc_user` 时抛出 `SQLSTATE[HY000] [1045] Access denied for user 'taichu'@'localhost'`。
  2. `GET /api/daily/fortune`（游客）同样在 `DailyFortune::getToday()` 查询 `tc_daily_fortune` 时命中 1045，公开日运仍不可用。
  3. 无 token 抽测 `GET /api/hehun/pricing`、`GET /api/liuyao/pricing`、`GET /api/tarot/history` 均只返回 `401 请先登录`；在登录前置未恢复前，本轮无法继续进入扣费 / 历史 / 分享闭环。
  4. `http://localhost:5173/daily` 与 `http://localhost/daily` 都连接拒绝，本轮没有可补测的前台页面实例。

### 处理结果
- 已将新的时间戳、报错落点与页面不可达证据补写到 `TODO.md` 的 `[15]` 条目，避免后续继续沿用旧的 `21:32` 证据。
- 本轮未做代码修复；下一步仍应先恢复 phpstudy 本机 MySQL 凭据与用户登录，再重跑八字 / 六爻 / 塔罗 / 合婚 / 每日运势闭环。

## 2026-03-19 第三十三轮执行记录

### 检查概览
- **检查时间**: 2026-03-19 05:55
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: 登录转化、每日运势公开链路，以及用临时 JWT 绕过登录后对积分/合婚/六爻入口做最小化复测。
- **核心发现**:
  1. `GET /api/health` 仍为 200，但 `POST /api/auth/phone-login`（真实 JSON 体）继续在 `tc_user` 查询阶段命中 `SQLSTATE[HY000] [1045] Access denied for user 'taichu'@'localhost'`。
  2. `GET /api/daily/fortune`（游客）继续在 `tc_daily_fortune` 查询阶段命中同一条 1045，公开日运仍不可用。
  3. 用临时 JWT 直探后，`GET /api/points/balance` 与 `GET /api/hehun/pricing` 同样回到 1045；`GET /api/liuyao/pricing` 返回 `code=500`，结合控制器首句 `User::find($userId)`，六爻入口也被同一数据库查询前置卡住。
  4. `http://localhost/daily` 与 `http://localhost:5173/daily` 继续连接拒绝，本轮依旧没有可直接补测的前台页面实例。

### 处理结果
- 已把最新时间戳与“临时 JWT 直探受保护入口”的影响范围补写到 `TODO.md` 的 `[15]` 条目。
- 本轮未做代码修复；下一步仍应先修正 phpstudy MySQL 凭据，再回到八字 / 六爻 / 塔罗 / 合婚 / 每日运势的扣费、历史、分享闭环。

## 2026-03-19 第三十四轮执行记录

### 检查概览
- **检查时间**: 2026-03-19 10:24
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: phpstudy 直连 `http://localhost:8080/api/...` 下重测登录前置、公开日运、八字首次免费、合婚免费预览与前台积分口径。
- **核心发现**:
  1. 登录前置已从旧的 MySQL 1045 阻塞恢复：`POST /api/sms/send-code` 能返回测试验证码，`POST /api/auth/phone-login` 可成功拿到 JWT。
  2. 公开日运不再是凭据问题，现稳定命中 `Unknown column 'lunar_date'`，属于 `daily_fortune` 字段结构错位。
  3. 八字首次免费链路被 `tc_bazi_record` 结构漂移拦住：`POST /api/paipan/bazi` 直接 `500`，`GET /api/paipan/history` 命中 `Unknown column 'location'`，结果 / 历史 / 分享无法闭环。
  4. 合婚免费预览可返回 `score=81 / level=good`，但 `GET /api/hehun/history?limit=5` 在调用前后都为空，确认存在“结果能看、历史不留”的失忆问题。
  5. 新用户注册奖励口径继续外溢：`GET /api/auth/userinfo`、`GET /api/points/balance`、`GET /api/points/history` 都显示新用户奖励实际记为 `0` 分，已与后台 `register_points` 读写错乱问题形成互证。

### 处理结果
- 已将本轮新增的 3 条占卜链路证据写回 `TODO.md` 的 `[30-4]` 章节，并把“注册奖励变 0 分”的前台实害证据补写到后台系统设置读写错乱条目。
- 本轮未做代码修复；下一轮建议优先排查 `daily_fortune.lunar_date`、`tc_bazi_record.location` 与合婚 free 结果未落历史三条链路。

## 2026-03-19 第三十五轮执行记录

### 检查概览
- **检查时间**: 2026-03-19 11:15
- **检查人员**: 资深占卜爱好者（AI自动化巡检）
- **检查范围**: 延续 phpstudy 直连口径，深查登录前置、塔罗主链路，以及六爻结果/历史/扣费闭环。
- **核心发现**:
  1. 老账号 `13800138000` 可正常登录并进入占卜链路，但未入库手机号 `13800138112`、`13800138113` 都在 `phone-login` 自动建号阶段直接返回“用户创建失败”，新用户首登再次被前置拦截。
  2. 塔罗旧历史/详情/公开分享仍可回看，但新抽牌 `POST /api/tarot/draw` 稳定 500，且失败后余额未减少；结合 `tc_points_record` 新增 `amount/balance/reason/description` 必填列而 `Tarot::draw()` 仍用旧写入口径，已形成高可信库表契约错位证据。
  3. 六爻本轮未发现新增阻塞：`pricing -> divination -> history -> detail` 链路可用，扣费 `15` 分与历史最新记录一致，AI 分析偶发降级但不影响主结果返回。

### 处理结果
- 已把“新用户登录自动建号失败”与“塔罗抽牌 500 但未误扣费”的新增证据补写到 `TODO.md` 的 `[30-4]` 章节。
- 本轮仍未修复代码；下一轮若继续占卜巡检，优先建议补抓新用户建号失败根因，以及塔罗 `tc_points_record` 写入口径与现表结构的真实异常栈。


