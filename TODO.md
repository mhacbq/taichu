## 待处理项目

### 🔴 高优先级
- [x] [运营] 2026-03-18 回归复测确认：用户详情页“用户名”字段仍回填手机号 `13800138005`，运营进入详情后仍无法确认用户的真实用户名，只能看到手机号口径 - 用户管理 / 用户详情 - 已统一详情接口 `username` 映射，并同步清洗嵌套 `user` 对象里的手机号口径。
- [x] [运营] 2026-03-18 已修复并复测：用户详情页“手动调积分”链路恢复；根因是本地 backend 容器长期运行旧代码，且 `AdminAuth` 中间件缺失 `sanitizeParams` 实现、`Notification` 存在静态方法签名冲突，现已同步容器源码并修复运行态致命错误，真实冒烟通过 `POST /api/admin/points/adjust` 的 `+1 / -1` 往返校验。
- [x] [运营] 2026-03-18 已修复并复测：黄历管理首屏恢复；根因是容器仍在运行旧版 `Almanac` 控制器，仍调用 `cal_days_in_month()`，现已切到工作区最新代码并通过真实冒烟验证 `GET /api/admin/content/almanac` 返回成功。
- [x] [运营] 2026-03-18 已修复并复测：神煞新增链路恢复；根因是容器仍在运行未包含 `check_rule` 后端兜底的旧版 `Shensha` 控制器，现已同步最新代码并通过真实冒烟验证新增/删除闭环。
- [x] [运营] 2026-03-18 已修复并复测：SEO 新增配置链路恢复；根因是容器仍在运行旧版 `Seo` 控制器，未使用动态表兼容保存逻辑，现已同步最新代码并通过真实冒烟验证新增/删除闭环。
- [x] [运营] 2026-03-18 已修复并复测：系统设置里的功能开关现已统一读写主表 `system_config` 并兼容旧布尔类型，`enable_feedback` 不再被旧 `tc_system_config` 阴影数据覆盖；同时已接通前台 `/config/client` 同步与 `Feedback/submit` 后端拦截，后台关闭后前台会实时隐藏/阻断反馈提交。



### 🟡 中优先级
- [x] [运营] 2026-03-18 回归复测：积分记录列表当前已能正确展示增减方向、变动后余额与变动原因，塔罗/八字/合婚等流水字段不再大面积缺失 - 积分管理 / 积分记录 - 本轮实测记录 #73/#71/#68 等均已显示正确正负值、余额与原因。
- [x] [运营] 2026-03-18 已修复：用户详情页已补齐“编辑资料”入口，运营现在可直接在后台维护昵称、邮箱、手机号、性别与头像；后端 `PUT /api/admin/users/:id` 与前端弹窗表单已联通。

- [x] [运营] 2026-03-18 回归复测：知识库分类表与“新增文章”下拉当前已恢复正常中文显示，运营能正确判断文章归类 - 网站内容 / 知识库文章 - 本轮接口返回 5 个分类，名称与描述均无乱码。
- [x] [运营] 2026-03-18 已修复：神煞管理列表已接入历史乱码修复与默认种子兜底，`?? / ?????` 记录可恢复为可读中文；已新增 `database/20260318_fix_shensha_display_encoding.sql` 供一次性回填历史脏数据，并补入容器 bootstrap，重启后也会自动补修历史脏数据。


- [x] [运营] 2026-03-18 回归复测：SEO 配置列表里的标题、描述、关键词当前已恢复正常中文显示，运营可直接判断页面文案是否正确 - 网站内容 / SEO 管理 - 本轮接口返回的 4 条配置均无乱码，新增/编辑/删除也已打通。
- [x] [运营] 2026-03-18 已修复：后台已新增独立“通知配置”页面，运营可维护通知默认开关、免打扰时段，并按用户 ID 发送测试通知；后端已补齐 `/api/admin/system/notification/*` 管理接口并接入现有通知设置表。





### 🟡 中优先级（体验问题）


### 🟢 低优先级（美观问题）


## 2026-03-18 代码审查（第五十二次）

### 检查范围
1. **前端逻辑检查** (frontend/src目录)：Vue组件、API调用、路由配置、状态管理
2. **管理端逻辑检查** (admin/src目录)：权限控制、数据表格、表单验证
3. **后台逻辑检查** (backend/app目录)：PHP控制器、数据库操作、API接口、中间件

### 检查结果 ✅

#### 前端代码 (frontend/src)
- Vue组件结构清晰，语法正确，无解析错误
- API请求封装良好 (request.js)，有统一的请求/响应拦截器
- 路由配置完整，使用meta字段实现权限控制 (router/index.js)
- 状态管理通过localStorage和组件通信，未使用复杂状态库
- console.log/warn/error用于调试日志，无泄漏风险

#### 管理后台 (admin/src)
- 管理后台路由配置完整，使用动态权限角色控制
- 权限控制通过meta字段和roles数组实现
- 数据表格和表单组件化良好，错误处理完善
- 请求拦截器统一处理认证Token和错误提示
- 用户详情页已添加编辑资料功能

#### 后端代码 (backend/app)
- PHP控制器使用ThinkPHP框架，遵循MVC架构
- 数据库操作使用查询构建器，防止SQL注入
- API返回格式统一使用 $this->success() 和 $this->error()
- 中间件实现正确 (Auth、AdminAuth)
- 异常处理已统一收口，使用全局Handler
- 参数验证使用filter_var和类型声明

### 发现的改进建议（非阻塞）

#### 🟢 低优先级（可优化项）
- [ ] [代码] 前端可考虑引入TypeScript增强类型安全 - 目前项目使用JS，暂无类型约束
- [ ] [性能] 前端大型组件可进一步拆分 - 如Hehun.vue超过2000行，可按功能模块拆分

### 结论
本次代码审查**未发现新的阻塞性问题**。代码整体质量良好，经过多轮迭代优化，关键业务逻辑已趋于稳定。已有的待处理问题均为之前记录的运营/UI问题。

---

## 2026-03-18 UI设计检查（第四十六次）


### 🟡 中优先级（体验问题）

### 🟢 低优先级（美观问题）









## 2026-03-18 数据库表优化与功能完善（第四十八次）

### 🔴 高优先级（核心架构优化）

#### 数据库表优化
- [ ] [架构] 删除或合并冗余的用户资料表 `tc_user_profile` - 该表字段完全可以合并到 `users` 主表中，当前两表冗余且需要 JOIN 查询，影响性能 - 建议：直接在 `users` 表添加 `gender/age/region/education/occupation` 等字段，然后删除 `tc_user_profile`
- [ ] [架构] 合并冗余的积分任务表 `tc_points_task` 与 `tc_task_log` - 两个表记录相同内容，容易造成数据不一致 - 建议：统一使用 `tc_task_log`，删除 `tc_points_task`，在迁移脚本中合并历史数据
- [ ] [架构] 检查并整合两个签到表 `checkin_record` 与 `tc_checkin_log` - 功能重复，维护困难 - 建议：确认业务逻辑后选择一个作为主表，另一个作为历史备表或删除

#### 敏感信息加密
- [x] [安全] 对 `tc_payment_config.api_key` 和 `tc_payment_config.api_cert` 实现字段级加密存储 - 当前直接存储明文，存在安全风险 - 已新增 `SensitiveConfigCrypt`，支付配置保存时自动加密，后台展示时仅返回脱敏值，运行时按需解密。
- [x] [安全] 对 `tc_payment_config.mch_id` 实现加密存储 - 商户号属于敏感信息 - 已和支付密钥一并改为加密落库，避免商户标识明文暴露。
- [x] [安全] 对 `tc_sms_config.secret_id` 和 `tc_sms_config.secret_key` 实现加密存储 - 短信服务凭据必须加密 - 已统一改为加密入库、脱敏展示、运行时解密读取。
- [x] [安全] 对 `tc_sms_config.sign_name` 加密存储 - 短信签名也是敏感标识 - 已纳入同一套加密读写链路。
- [ ] [安全] 考虑对 `users.phone` 实现加密存储或脱敏处理 - 用户手机号属于 PII（个人可识别信息）


### 🟡 中优先级（功能完善）

#### 未实现的功能表
- [ ] [功能] 激活流年运势功能 - `tc_yearly_fortune` 表已创建但代码中未使用 - 建议：
  - 在前端添加"流年运势"功能入口
  - 创建流年运势计算逻辑
  - 接入 AI 进行深度分析
  - 参考表字段：user_id, year, overall_score, career, wealth, love, health, analysis
  
- [ ] [功能] 激活取名功能 - `tc_qiming_record` 表已创建但代码中未使用 - 建议：
  - 在前端添加"取名建议"功能入口
  - 创建取名建议生成逻辑（基于八字五行）
  - 集成 AI 进行名字质量评测
  - 参考表字段：user_id, bazi_data, suggested_names, rating_scores, analysis

#### 表字段完善
- [ ] [数据] 在 `users` 表补充验证码相关字段 - 当前必须查 `tc_sms_code` 表才能验证状态 - 建议：添加 `last_sms_code_time`, `sms_code_attempts` 字段以优化查询
- [ ] [数据] 在 `points_records` 表补充更多业务分类 - 当前 `action` 字段值不统一 - 建议：建立 action 的枚举定义，并补充缺失的业务场景分类
- [ ] [数据] 在 `bazi_records` 表添加 `ai_analysis_model` 字段 - 记录使用的 AI 模型（GPT-3.5/GPT-4/Claude等），便于追溯和对比结果

### 🟢 低优先级（性能与运维）

#### 性能优化与监控
- [x] [性能] 为 `points_records` 表建立按用户 ID 的分区索引 - 该表是高频写表，Year 2 预计 ~1M 行 - 已新增 `database/20260318_add_points_recharge_sms_indexes.sql`，补齐 `tc_points_record(user_id, created_at)` 复合索引。
- [ ] [性能] 为 `operation_logs` 表建立定期清理策略 - 该表增长快，Year 2 预计 ~100MB - 建议：仅保留最近1年的操作日志，老数据定期归档到文件系统或备份库
- [ ] [性能] 为 `tc_checkin_log` 表建立分区表结构 - 签到数据量大且增长快 - 建议：按日期分区，方便定期清理或归档
- [ ] [性能] 引入 Redis 缓存策略 - 避免重复查询配置表、模板表等 - 建议：

  - 缓存 `system_config` 全表（1小时过期）
  - 缓存 `ai_prompts`（按 category 分组）
  - 缓存 `tarot_cards` 和 `tarot_spreads`
  - 缓存用户的 VIP 状态（30分钟过期）
  
- [x] [性能] 对高频查询表建立必要的复合索引 - 已在 `database/20260318_add_points_recharge_sms_indexes.sql` 中补齐 `tc_recharge_order(user_id, status, created_at)`，并顺带为短信验证码清理增加 `tc_sms_code(expire_time, is_used)` 索引。
  - `bazi_records`: `KEY idx_user_created (user_id, created_at)`
  - `tc_tarot_record`: `KEY idx_user_spread_created (user_id, spread_type, created_at)`
  - `recharge_orders`: `KEY idx_user_status_created (user_id, status, created_at)`


#### 数据归档与清理
- [x] [运维] 为 `tc_sms_code` 表建立过期验证码清理 - 定期删除 `expire_time` 已过期的记录 - 已在 `DailyTask` 中增加 `SmsCode::purgeExpired(7)`，并补齐配套清理索引。


## 2026-03-18 UI设计检查（第四十九次）

### 🔴 高优先级（功能性问题）



### 🟡 中优先级（体验问题）





### 🟢 低优先级（美观问题）


## 2026-03-18 代码审查（第五十一次）

### 检查范围
1. **前端逻辑检查** (frontend/src目录)：Vue组件、API调用、路由配置、状态管理
2. **管理端逻辑检查** (admin/src目录)：权限控制、数据表格、表单验证
3. **后台逻辑检查** (backend/app目录)：PHP控制器、数据库操作、API接口、中间件

### 检查结果 ✅

#### 前端代码 (frontend/src)
- Vue组件结构清晰，语法正确，无解析错误
- API请求封装良好 (request.js)，有统一的请求/响应拦截器
- 路由配置完整，使用meta字段实现权限控制 (router/index.js)
- 状态管理通过localStorage和组件通信，未使用复杂状态库
- 未发现未使用的变量或导入

#### 管理后台 (admin/src)
- 管理后台路由配置完整，使用动态权限角色控制
- 权限控制通过meta字段和v-permission指令实现
- 数据表格和表单组件化良好，错误处理完善
- 请求拦截器统一处理认证Token和错误提示

#### 后端代码 (backend/app)
- PHP控制器使用ThinkPHP框架，遵循MVC架构
- 数据库操作使用查询构建器，防止SQL注入
- API返回格式统一使用 $this->success() 和 $this->error()
- 中间件实现正确 (Auth、AdminAuth)
- 异常处理已统一收口，使用全局Handler
- 参数验证使用filter_var和类型声明

### 发现的改进建议（非阻塞）

#### 🟢 低优先级（可优化项）
- [ ] [代码] 前端可考虑引入TypeScript增强类型安全 - 目前项目使用JS，暂无类型约束
- [x] [代码] 后端可增加更多单元测试覆盖 - 当前以集成测试为主 - 已新增 `backend/tests/run.php` 轻量级测试入口，并补齐敏感配置加解密、积分流水归一化两组关键单元测试。

- [ ] [性能] 前端大型组件可进一步拆分 - 如Hehun.vue超过2000行，可按功能模块拆分

### 结论
本次代码审查**未发现新的阻塞性问题**。代码整体质量良好，经过多轮迭代优化，关键业务逻辑已趋于稳定。已有的待处理问题均为之前记录的运营/UI问题。

---

## 2026-03-18 UI设计检查（第五十次）

### 🔴 高优先级（功能性问题）


### 🟡 中优先级（体验问题）



## 2026-03-18 UI设计检查（第五十二次）


### 🔴 高优先级（功能性问题）

### 🟡 中优先级（体验问题）

### 🟢 低优先级（美观问题）


## 2026-03-18 UI设计检查（第五十三次）

### 🔴 高优先级（功能性问题）


### 🟡 中优先级（体验问题）



### 🟢 低优先级（美观问题）


## 2026-03-18 占卜深度体验检查（第三十次）

### 🔴 高优先级（逻辑错误/准确性问题）
- [ ] [占卜] 验证码发送接口当前直接 500，`POST /api/sms/send-code` 因缺少 `tc_sms_code` 表报错，登录前置流程被卡死 - 占卜通用 / 登录入口 - 游客无法正常进入八字、六爻、塔罗与合婚等需登录功能。
- [ ] [占卜] 八字流年深度分析失败仍照常扣费，本轮复测 `POST /api/fortune/yearly` 返回 `HTTP 200 + code 500`，但 `tc_points_record` 仍写入 `yearly_fortune -30` - 八字排盘 / 流年运势深度分析 - 用户既拿不到结果又损失积分，属于准确性与计费双重事故。
- [ ] [占卜] 八字大运 K 线图接口崩溃后仍扣 30 积分，`/api/fortune/dayun-chart` 触发 `DayunFortuneService::calculateYearScoreInDayun(int $dayunScore, ...)` 的 float/int 类型错误 - 八字排盘 / 大运走势图 - 结果空白且失败后不回滚积分，严重影响付费信任。
- [ ] [占卜] 六爻起卦接口虽然返回 200 和卦象摘要，但两次实测 `id` 都是 `null`，随后 `/api/liuyao/history` 仍为空，记录保存链路实际未落库 - 六爻占卜 / 历史记录 - 用户无法回看主卦、变卦与后续解读，专业复盘链路中断。
- [ ] [占卜] 塔罗三种牌阵都能抽牌，但 `POST /api/tarot/interpret` 在单张牌、三张牌、凯尔特十字上全部返回“解读失败，请稍后重试” - 塔罗占卜 / 解牌主链路 - 用户只能看到牌面，拿不到核心解读结果。

### 🟡 中优先级（体验问题）
- [ ] [占卜] 六爻返回结果当前只给简写卦名（如“兑兑”“兑坎”）和一段摘要，缺少结构化展示的变卦、动爻、六亲、六神、卦辞字段 - 六爻占卜 / 结果展示 - 建议把核心卦理字段单独返回并在结果区显式排盘，方便专业用户核验用神旺衰。

### 🟢 低优先级（专业性优化）
- [ ] [占卜] 八字解读虽然四柱与喜用神已基本可读，但文案仍偏通用模板，对庚金日主生于巳月的调候、财官食伤透藏层次展开不足 - 八字排盘 / 命理解读 - 优化方向是补充调候依据、格局取舍与关键大运转折说明。
- [ ] [占卜] 塔罗抽牌接口的关键词字段大多直接复用正逆位牌义，逆位文案高频以“由于……”开头，韦特体系的象征层次和牌位语境还不够细 - 塔罗占卜 / 牌义展示 - 优化方向是补齐更像专业牌师口径的关键词、元素隐喻与宫廷牌细化解释。