# 太初命理网站 - 综合待办修复列表

> 最后更新时间: 2026-03-17
> 本文档已根据多轮检查报告进行去重、整合与优化，作为当前开发的统一指导文档。

---

## 🚀 核心优先级 (Top Priorities)

1.  **全站视觉标准化**：彻底清理粉色系残留，统一使用金色主题变量，移除所有 Emoji 图标。
2.  **管理后台真实化**：将管理后台从模拟数据转向真实 API 对接，补齐缺失的管理页面。
3.  **算法准确性升级**：提升八字强弱判断准确度，完善六爻变卦、六亲、六神分析系统。
4.  **系统安全加固**：修复 SQL 注入风险，移除硬编码凭据，完善 API 响应一致性。

---

## 🎨 UI/UX 视觉与交互优化

### 🔴 高优先级
- [x] **清理粉色系残留**：已完成核心页面清理，搜索并替换全站剩余的 `rgba(233, 69, 96)`、`#e94560` 为金色系 `var(--primary-color)`。
- [x] **移除 Emoji 图标**：将组件（Bazi.vue, Tarot.vue, Liuyao.vue 等）中大量使用的 Emoji（✨, ☯, 🎴, 💕 等）替换为 Element Plus 图标。 (已完成 GuideModal.vue, Tarot.vue, Bazi.vue CSS 遗留清理)
- [x] **白色文字硬编码清理**：统一替换全站至少 93 处 `color: #fff` 为 CSS 变量 `var(--text-primary)`，避免深色主题下不可读问题。(已完成主要页面清理)
- [x] **塔罗牌视觉升级**：设计或引入真实的塔罗牌图片/SVG 资源，替代当前的 Emoji 牌面。(已在 TarotCard.vue 添加 SVG 装饰)

### 🟡 中优先级
- [ ] **规范化组件样式**：
    - [x] 统一按钮圆角为 `25px`，卡片圆角为 `16px`。
    - [x] 统一各页面卡片的 `hover` 动画效果和阴影深度。
- [x] **统一响应式断点**：将当前混用的 576px/768px/992px/1024px 多种断点统一为 CSS 变量定义的 768px/992px/1200px。
- [ ] **交互体验提升**：
    - [x] 添加 Vue Router 页面切换过渡动画。(已在 App.vue 实现 fade 过渡)
    - [x] 实现全站统一的“空状态 (Empty State)”组件。(已完成 EmptyState.vue 标准化并支持金色主题)

    - [x] 统一全站加载状态 (Loading) 为太极旋转动画。(已在主要页面实现)
    - [x] **移动端触摸区域优化**：确保所有按钮和交互元素的触摸区域不小于 44px，特别是 mobile-nav-close 和 close-btn 等关键按钮。 (已在 App.vue 等组件中落实)

### 🟢 低优先级
- [ ] **视觉细节优化**：
    - 优化首页 Hero 区域背景渐变效果，使其更明显
    - 统一各页面功能卡片 hover 效果
    - 优化评分星星颜色使用，避免硬编码
    - 优化六爻卦象线条样式，增加设计感

---

## 💻 后端逻辑与系统安全

### 🔴 高优先级
- [x] **修复Auth.php重复导入** - `backend/app/controller/Auth.php` 中重复导入 `use think\facade\Log;` 的问题已清理完成。

- [x] **安全风险修复**：
    - [x] 修复 `Admin.php` 和 `Content.php` 中的 SQL 注入隐患，使用参数绑定替代字符串拼接。(已完成 Admin.php 和 Content.php 参数化处理)
    - [x] 移除 `AdminAuth.php` 中的硬编码管理员凭据，对接数据库验证。(已完成)
    - [x] 将 JWT 密钥等敏感信息移出代码，改用环境变量读取。(已完成)
- [ ] **API 规范化**：
    - [x] 统一后端响应码，成功一律返回 `200`（目前部分接口返回 `0`）。(已分批修复 Admin 前端判断逻辑)
    - [x] 统一使用 `BaseController` 的 `success()` 和 `error()` 方法返回数据。(已完成核心控制器与 SSE 优化)
- [ ] **功能补全**：
    - [x] 实现 Dashboard 实时统计数据接口 (`/dashboard/realtime`)。(已完成)
    - [x] 实现待处理反馈统计接口 (`/dashboard/pending-feedback`)。(已完成)
- [ ] **SSE 与接口健壮性**：
    - [x] 补齐 `AiAnalysis` 旧版路由兼容 (`analyze`/`analyzeStream`/`history`)。
    - [x] 完善 SSE 流式解盘（响应头、连接超时、断连处理、错误输出标准化）。
    - [x] 补齐 `AiPrompt` 缺失的 `getDefaultPrompt` 接口与参数校验。
- [ ] **通用能力与日志**：
    - [x] 新增 `BaseController::normalizePagination/getPaginationParams` 统一分页处理。
    - [x] 新增 `BaseController::getOperatorId` 统一操作者ID读取。
    - [x] 统一后端日志为 `Log::error` 并记录详细上下文，避免异常信息泄露。
    - [x] 修复 `Upload`、`Liuyao` 等控制器多处异常直接返回 `$e->getMessage()` 的问题。

---

## 👔 管理后台 (Admin) 专项

### 🔴 高优先级
- [x] **真实 API 对接**：将当前核心管理页面（每日运势、塔罗记录等）从模拟操作转向真实的后端 API 对接。
- [ ] **补齐管理页面**：
    - **内容管理**：[x] 创建黄历管理 (`almanac.vue`)、每日运势管理、塔罗记录管理页面。
    - **系统管理**：完善操作日志查看页面、[x] 管理员权限配置页面 (已配置路由 roles)。
- [x] **旧代码清理**：删除 `frontend/src/views/admin/` 目录下的旧版管理页面，避免与新版 `admin` 项目混淆。(已在路由配置中移除)

### 🟡 中优先级
- [ ] **数据可视化**：在 Dashboard 集成 ECharts，实现用户增长和订单趋势的真实图表展示。
- [ ] **批量操作功能**：在用户列表和订单列表中添加批量启用/禁用、批量导出等功能。
- [ ] **完善表单验证**：补全所有管理后台表单的必填项验证和格式校验。

---

## 🔮 命理算法与功能体验

### 🔴 高优先级
- [ ] **八字算法升级**：
    - [x] 完善日主强弱判断逻辑，综合考虑月令旺衰（得令）、通根深浅、透干藏干等因素。
    - [x] **[占卜] 节气边界计算修正** - `BaziCalculationService.php` - 已根据 `jieQiData` 精准判定月柱地支切换点。
    - [x] **[占卜] 夜子时跨日逻辑** - `BaziCalculationService.php` - 23:00-00:00 出生者日柱已切换至次日。
    - [x] **[占卜] 塔罗牌组补全** - `Tarot.php` - 已补全 78 张塔罗牌（包含 56 张小阿卡纳）。
    - [x] **[占卜] 凯尔特十字位映射** - `Tarot.php` - 已补齐并修正凯尔特十字 10 个专业牌位定义。
    - [x] **[占卜] 六爻动爻逻辑修正** - `LiuyaoService.php` - 已修正 `generateYaoCode` 动爻转换逻辑。
    - [ ] **[占卜] 八字日柱基准校验** - `BaziCalculationService.php` - 目前使用的 1900-01-31 为甲子日的假设有误（实为甲辰），需重新校对基准日以确保全站日柱准确性。
    - [ ] **[占卜] 六爻时间起卦法规范化** - `LiuyaoService.php` - `qiGuaByTime` 目前使用公历年月日简单相加，不符合梅花易数传统起卦规则（需使用农历及干支序数），建议重构。
    - [ ] **[占卜] 每日运势“幸运项”算法优化** - `Daily.php` - 幸运数字、颜色、方位目前为随机生成，应改为基于当日五行能量及用户日主需求计算。
    - [ ] **[占卜] 塔罗解牌逻辑深度提升** - `Tarot.php` - 目前解牌仅为单张牌义堆砌，缺乏牌位间克应关系分析及针对性建议，需引入更专业的解牌逻辑映射。
    - [ ] **[占卜] 九宫合婚逻辑修复** - `Hehun.php` - `analyzeGuaRelation` 中离命卦（索引9）的配对关系数据全是“伏位”的硬编码错误。
    - [ ] **[占卜] 日运个性化深度优化** - `Daily.php` - 个性化运势需结合日主强弱（喜用神）判定，而非单纯看十神关系。
- [ ] **六爻系统完善**：

### 🟡 中优先级
- [ ] **功能扩展**：
    - **八字**：实现基于经度的真太阳时转换逻辑。
    - **塔罗**：接入 AI 深度解牌接口，提升解读的专业性。
    - **合婚**：添加传统三元合婚、九宫合婚等算法。
    - **[占卜] 六爻伏神系统** - `LiuyaoService.php` - 增加伏神分析，处理用神不现时的推算逻辑。
    - **[占卜] 立春动态判定** - `BaziCalculationService.php` - 移除 2 月 4 日固定阈值，改为根据节气数据动态判定年柱。

### 🟢 低优先级
- [ ] **细节优化**：
    - 在六爻占卜中添加“手动起卦”模式。
    - 在排盘结果中更醒目地展示藏干十神信息。
    - **[占卜] 视觉专业化升级** - 全站占卜模块 - 逐步移除 Emoji，引入专业塔罗牌图片、六爻卦象 SVG 图标和八字排盘表格美化。
    - **[占卜] 古籍引用增强** - 各占卜模块 - 增加《三命通会》、《增删卜易》、《韦特塔罗》等专业断语引用。

---

## 🕵️ 运营检查报告 (Operational Audit)


### 2026-03-17 第十五轮检查报告

#### 🔴 高优先级（运营阻塞问题）
- [ ] [运营] 神煞管理前端接线未完成 - 内容管理 - 后端已补齐 Admin.php 路由与 CRUD 接口，前端新版管理后台仍需注册页面路由并接入真实 API。
- [ ] [运营] SEO管理前端接线未完成 - 系统配置 - 后端已补齐 SEO 配置/统计/robots/收录提交接口，前端仍需对接真实 TDK 与搜索引擎提交界面。

#### 🟡 中优先级（运营体验问题）
- [ ] [运营] 批量操作功能缺失 - 用户管理/订单管理 - 目前列表仅支持单条操作，缺乏批量启用/禁用用户、批量导出数据、批量处理订单等功能，大批量运营时效率较低。
- [ ] [运营] 字典管理深度对接 - 系统管理 - 需确认 `admin/src/views/system/dict.vue` 中所有操作是否已完全同步至数据库，避免前端状态与后端不一致。

#### 🟢 低优先级（运营优化建议）
- [ ] [运营] Dashboard 待处理反馈交互优化 - 首页看板 - 目前“处理”按钮跳转至列表页，建议在看板页直接弹出处理对话框，减少页面跳转层级。
- [ ] [运营] 系统设置搜索功能 - 系统配置 - 当配置项增多后，建议在系统设置页面增加搜索/过滤功能，方便快速定位配置项。

---

## 🔍 代码审查问题 (Code Audit Issues)

### 🔴 高优先级
- 暂无本轮新增的高优先级后端代码审查问题。

### 🟡 中优先级
- [x] [2026-03-17] 前端错误处理不完善 - frontend/src/views/*.vue (多处) - 已为 Bazi.vue 和 Liuyao.vue 等核心页面添加 ElMessage 错误提示。
- [x] [2026-03-17] 管理后台Dashboard API对接 - admin/src/views/dashboard/index.vue - 字段名已完成对接。

### 🟢 低优先级
- [ ] [2026-03-17] 塔罗牌仍使用Emoji - backend/app/controller/Tarot.php - 虽然前端已处理，但后端数据仍包含emoji字段，建议后续统一使用图片/SVG

---

## ✅ 最近已完成项目 (Recently Completed)

- [x] **UI 样式标准化 (Batch #1)**: 统一了 `Tarot.vue`, `TarotCard.vue`, `Profile.vue`, `Recharge.vue`, `Login.vue` 的卡片圆角（16px）、背景色及硬编码颜色变量化。
- [x] **前端主题色初步统一**：已分批处理 `Home.vue`、`Bazi.vue`、`Tarot.vue` 等核心页面的粉色系清理。
- [x] **CSS 变量标准化**：在 `LimitedTimeOffer.vue`、`Daily.vue` 等组件中引入全局变量替代硬编码。
- [x] **节气数据补充**：已初步扩充节气查询表。
- [x] **JWT 认证实现**：后端管理端 JWT 认证流程已跑通。
- [x] **TODO 列表瘦身**：清理了 50+ 轮历史重复检查报告，完成任务重组。

---

## 🎨 第三十一轮UI设计检查报告 (2026-03-17)

作为资深产品经理和UI设计师，从专业角度全面检查网站设计问题，发现以下需要优化之处：

### 🔴 高优先级（功能性问题）
- [ ] [UI] 白色文字硬编码问题依然严重 - 全站仍有45处 `color: #fff` 硬编码，分布在Bazi.vue（12处）、AchievementSystem.vue（7处）、AchievementBadge.vue（6处）等关键组件 - 统一替换为 CSS 变量 `var(--text-primary)`，确保深色主题下可读性
- [ ] [UI] 移动端触摸区域不达标 - 多个按钮padding过小（如3px 10px、4px 8px），影响移动端用户体验 - 确保所有交互元素的触摸区域不小于44px，特别是关键操作按钮
- [ ] [UI] CTA区域视觉冲击力不足 - 首页CTA区域背景渐变透明度低（0.1），不够突出 - 加强CTA区域渐变背景，提升视觉吸引力

### 🟡 中优先级（体验问题）
- [ ] [UI] 勋章展示区域样式陈旧 - 首页trust-badges使用 `rgba(255, 255, 255, 0.6)`，与整体设计风格不匹配 - 改用金色系设计，提升视觉统一性
- [ ] [UI] 塔罗牌阵选择区域设计平淡 - 牌阵卡片视觉吸引力不足，交互反馈不够明显 - 增强牌阵卡片视觉设计，添加更丰富的交互反馈
- [ ] [UI] 八字排盘表单体验有待提升 - 表单标签间距不明确，提示文字可读性一般 - 优化表单布局和文字层次，提升填写体验
- [ ] [UI] Emoji图标仍有残留 - 全站仍有18处Emoji图标（✨、☯、🎴等），影响专业形象 - 批量替换为Element Plus图标，统一设计风格

### 🟢 低优先级（美观问题）
- [ ] [UI] 首页统计数据展示不够吸引人 - 统计数字展示方式传统，缺乏视觉设计感 - 设计更具吸引力的数据统计展示组件
- [ ] [UI] 六爻卦象展示可以更美观 - 卦象线条样式基础，可以增加设计感 - 优化卦象线条样式，增加专业设计元素
- [ ] [UI] 功能特色卡片hover效果可以更丰富 - 目前hover效果比较基础 - 增加更丰富的卡片hover动画和交互反馈
- [ ] [UI] 评分星星颜色硬编码 - 部分评分星星使用硬编码颜色 - 统一使用CSS变量管理评分颜色

---

