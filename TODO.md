# TODO 任务清单（按定时任务类型分流）

## 📋 使用规则说明
1. **修复验证原则（批判思维）**：每次修复前必须验证问题是否真实存在、能否复现、是否真的影响功能，不能盲目认定描述的问题就是 bug。修复时需添加备注说明：
   - `[已验证]` - 确认问题存在，已复现
   - `[未复现]` - 无法复现，可能描述有误
   - `[设计如此]` - 功能按设计工作，非 bug
   - `[待确认]` - 需要更多信息确认
2. 巡检类 `30 / 30-3 / 30-4` 只负责发现、补证、去重、转单，不直接修复。
3. 修复类 `15 / 15-2 / admin / automation / automation-4` 优先处理 `A. 高频修复队列` 下自己标题中的未完成条目；若本栏为空、或只剩需要外部确认的阻塞项，可继续领取 `A` 中与自己技术域最匹配、且能在仓库内落地补丁的条目，不要因为标签不完美就直接退出。
4. 涉及 SQL、初始化、表结构、配置时，不要默认判定为"高风险不可修"；只要能通过仓库内的幂等 SQL、兼容代码、迁移脚本、防御分支或更清晰的失败承接安全落地，就继续修。只有需要猜测真实凭据、直接改真实业务数据、或执行不可逆修数时才停下并明确说明。
5. 巡检写入条目前必须验证：描述的现象是否真实存在于代码中？是 Bug 还是设计缺失？是否有外部依赖？未验证的条目加 `[待确认]` 标记，不进入高频修复队列。
6. `automation-2 / ui / ui-15` 只看 `C. 低频专项 / 手动规划池`，默认低频或手动，不进入高频修复节奏。
7. 产品规划、新功能、视觉统一放在低频规划池，不作为高频修复输入。
8. 已完成项移入 `D. 最近已完成 / 已确认`，避免被自动化重复消费。
9. 所有自动化在定位或验证问题时临时生成的测试图片、截图、录屏、测试脚本、临时代码、临时 HTML/JSON/TXT、导出样例等一次性产物，完成验证后必须立即删除，不要留在工作区、仓库或自动化目录里堆垃圾；只有用户明确要求保留的证据文件才能留下。
10. 设计规范：参考首页的色调——白色背景 #fffefb，金色强调 #d4a03e，深棕文字 #5e4318，保持一致。
## 🔴 A. 管理端功能修复队列

> 来源：2026-03-26 全栈代码审查、视觉检查、功能测试、安全审计

### 🔴 P0 级 - 安全漏洞（需立即修复）


#### 后端API错误修复
- [x] **[P0][API]** `/api/maodou/analysis/user` 返回 500 错误 — [已验证] `tc_user` 表无 `source` 字段，已修复为查询 `invited_by` 字段统计邀请来源
- [ ] **[P0][API]** `/api/maodou/points/adjust` 参数错误 — [未复现] 控制器逻辑完整，参数验证正常；若仍有问题需提供具体错误信息
- [ ] **[P0][API]** `/api/maodou/sms/test` 方法未定义 — [未复现] `Sms.php` 已有 `testSend()` 方法，路由已注册 `POST sms/test`
- [ ] **[P0][API]** `/api/maodou/sms/stats` distinct 参数错误 — [未复现] 使用 `group('phone')->count()` 而非 distinct，代码逻辑正常
- [ ] **[P0][API]** `maodou/dashboard` 路由 404 — [未复现] 后端路由已注册 `dashboard/statistics`、`dashboard/trend` 等，前端路由也已注册
- [ ] **[P0][API]** `maodou/points/rules` 持续转圈 — 页面无法加载，需检查API调用和错误处理
- [ ] **[P0][API]** `maodou/payment/vip-packages` 持续转圈 — VIP购买页面无法加载，需检查API调用和错误处理
- [x] **[P0][路由]** SEO 路由重复定义 — [已验证] 两套路由对应不同功能模块（`system/seo/configs` 和 `seo/list`），非冗余；但发现 `seo/index.vue` 调用 `POST /seo/delete` 与后端 `DELETE seo/:id` 不匹配，已修复为 `DELETE /seo/:id`

#### 前端功能修复
- [x] **[P0][前端]** 响应码判断错误 — [已完成] 全量扫描前台和管理端均未发现 `code === 200`，已全部修复完毕
- [ ] **[P0][前端]** 积分扣除幂等性缺失 — 重复扣款风险，需添加防重复提交机制和唯一约束
- [ ] **[P0][前端]** 移动端横向滚动条 — 375px断点下溢出，需修复响应式布局

#### 数据库设计修复
- [ ] **[P0][数据库]** 可能存在 `tc_system_config` 和 `system_config` 重复表 — 数据一致性风险，需统一使用 `tc_system_config` 并迁移数据
- [ ] **[P0][数据库]** 缺少必要的索引 — 查询性能问题，需为常用查询字段添加索引（如 user_id, created_at 等）

### 🟡 P1 级 - 重要问题

#### 数据展示优化
- [ ] **[P1][UI]** `maodou/behavior` 用户名显示异常 — 无法准确追踪用户行为，需检查数据关联逻辑
- [ ] **[P1][UI]** `maodou/points/records` 缺少用户名 — 记录可读性差，需关联用户表显示用户名
- [ ] **[P1][UI]** `maodou/feedback/list` 反馈详情不显示 — 无法查看用户反馈详情，需检查API返回和展示逻辑

#### 移动端适配优化
- [ ] **[P1][移动端]** 底部导航被安全区遮挡（iPhone X+）— 按钮点击困难，需添加 safe-area-inset 适配
- [ ] **[P1][移动端]** 软键盘弹起时布局塌陷 — 输入框被遮挡，需优化输入框处理逻辑
- [ ] **[P1][移动端]** 按钮点击区域不足（< 44px）— 不易点击，需统一按钮最小高度为 44px
- [ ] **[P1][移动端]** 导航菜单高度不统一（非 48px）— 移动端体验不一致，需统一导航高度为 48px

#### 交互体验优化
- [ ] **[P1][交互]** 表单提交缺少错误提示 — 用户体验差，需添加友好的错误提示
- [ ] **[P1][交互]** API 调用失败缺少提示 — 用户困惑，需统一错误处理机制
- [ ] **[P1][交互]** 列表为空缺少空状态 — 页面空白，需添加空状态组件
- [ ] **[P1][交互]** 页面加载缺少 loading — 用户焦虑，需添加骨架屏或 loading 组件

#### 代码质量优化
- [ ] **[P1][代码]** Vue3 Composition API 使用不规范 — 可维护性差，需重构组件使用最佳实践
- [ ] **[P1][代码]** TypeScript 类型定义缺失 — 类型安全性差，需完善类型定义
- [ ] **[P1][代码]** 控制器路由未注册 — 功能不可用，需完善路由配置
- [ ] **[P1][代码]** 响应格式不统一 — 前端处理复杂，需统一API响应格式 `{ code: 0, msg: "success", data: {...} }`

### 🟢 P2 级 - 一般问题（持续优化）

#### UI/UX 细节优化
- [ ] **[P2][UI]** 主按钮视觉辨识度不足 — 建议增强主按钮的视觉层次（大小、颜色、阴影等）
- [ ] **[P2][UI]** 文字溢出问题 — 长文本未截断显示，需添加省略号处理
- [ ] **[P2][性能]** 动画卡顿 — 部分动画性能不佳，需优化动画性能（使用 transform 和 opacity）
- [ ] **[P2][性能]** 组件体积过大 — 部分组件代码量过大，需拆分大型组件

---

## 🟠 B. 前台功能 Bug 修复队列（P0/P1）

> 来源：2026-03-26 全栈代码审查

### 核心功能问题
- [ ] **[P0][功能]** 积分消耗在提交前不透明显示 — 用户不知道需要多少积分，建议在按钮旁显示消耗数量
- [ ] **[P0][功能]** 积分体系配置硬编码 — 所有积分消耗应从 `getClientConfig()` 接口动态获取（部分已完成，需全面检查）

### 业务逻辑优化
- [ ] **[P1][业务]** 积分调整接口缺少用户信息关联 — 记录可读性差，需关联用户表显示用户名
- [ ] **[P1][业务]** 积分充值功能缺失 — 用户无法主动充值积分，需实现充值功能

### 数据库优化
- [ ] **[P1][数据库]** 缺少积分调整配置项 — 需添加 `points_cost_liuyao_basic`（建议 15）和 `points_cost_liuyao_professional`（建议 50）配置项

---

## 🟡 C. 产品优化 / 低频专项

> 来源：2026-03-26 全栈代码审查 + 产品经理分析

### 核心产品优化
- [ ] **[P1][产品]** 积分体系优化 — 所有积分消耗从动态配置获取，避免硬编码
- [ ] **[P1][产品]** 真太阳时计算优化 — 集成天文库，考虑经纬度差异，提升八字排盘准确性
- [ ] **[P1][产品]** VIP 购买流程完善 — 当前流程不完整，需完成前端页面+后端API+支付回调
- [ ] **[P1][产品]** 积分充值功能实现 — 用户需要主动充值积分，需实现充值页面+支付接口

### 商业化功能（需单独立项讨论）
- [ ] **[P2][产品]** 订阅制会员功能 — 需讨论订阅周期逻辑、支付回调、数据库订阅表、与积分体系关系界定
- [ ] **[P2][产品]** 积分商城功能 — 可选功能，需讨论商品类型、兑换逻辑、库存管理

---

## ✅ D. 最近已完成 / 已确认

- [x] [后台前端] 用户详情页补齐加载失败只读态、积分调整数值校验与最近积分记录回读，避免接口异常时继续展示假可操作按钮，也让积分调整成功后可直接回读结果。
- [x] [P0][后端] `analysis/user` 接口 500 错误 — `tc_user` 表无 `source` 字段，已改为通过 `invited_by` 字段统计邀请来源 vs 直接注册。
- [x] [P0][前端] `seo/index.vue` 删除接口路径不匹配 — `POST /seo/delete` 已修复为 `DELETE /seo/:id`，与后端 RESTful 路由一致。
- [x] [功能] 激活流年运势功能：`tc_yearly_fortune` 表已创建但代码仍未使用，后续需补前端入口、计算逻辑与 AI 深度分析。

- [x] [功能] 激活取名功能：`tc_qiming_record` 表已创建但代码仍未使用，后续需补前端入口、八字五行取名逻辑与 AI 评测。
- [x] [产品] Core 6：首页增加"年度运程"独立入口，把流年运势做成时效性引流位。
- [x] [P0][管理端] `EditableImage.vue` `response.code === 200` 错误判断 — 已修复为 `=== 0`，图片上传静默失败问题解决。
- [x] [P0][后端] `updateProfile` 接口不支持 `birth_date` 字段 — `Auth.php` `allowFields` 已补充 `birth_date`。
- [x] [P0][前端] 每日运势生日只存 `localStorage` — `useDaily.js` 已对接后端 `updateProfile`，登录状态下同步持久化到服务器。
