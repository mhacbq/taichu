## 待处理项目

### 🔴 高优先级
- [ ] [运营] 2026-03-18 回归复测确认：用户详情页“用户名”字段仍回填手机号 `13800138005`，运营进入详情后仍无法确认用户的真实用户名，只能看到手机号口径 - 用户管理 / 用户详情 - 需统一详情接口 `username` 映射，避免手机号继续覆盖用户名字段。
- [ ] [运营] 用户详情页“手动调积分”弹窗可打开，但提交最小化 `+1` 积分后仍提示“调整积分失败，请稍后重试”；对应 `POST /api/admin/points/adjust` 的 HTTP 层返回 200、业务码返回 500，人工补偿/扣减积分仍无法落地 - 用户管理 / 用户详情 / 积分调整 - 需继续排查积分调整服务的业务失败原因，并补充明确错误提示。
- [ ] [运营] 黄历管理首屏列表接口 `GET /api/admin/content/almanac` 仍返回“获取黄历列表失败，请稍后重试”，后台页会直接进入只读保护；虽然单条新增/编辑/删除接口可用，但运营真实在页面中仍无法维护黄历 - 内容管理 / 黄历管理 - 需优先修复黄历列表查询链路并恢复页面加载。
- [ ] [运营] 神煞管理列表与选项接口可正常加载，但新增神煞时 `POST /api/admin/system/shensha` 仍返回“保存神煞失败，请稍后重试”，运营无法补录新的神煞数据 - 内容管理 / 神煞管理 - 需排查新增写链路、字段校验与后端保存逻辑。
- [ ] [运营] SEO 管理页列表与 Robots 配置可正常读取，但新增 SEO 配置时 `POST /api/admin/system/seo/configs` 仍返回“保存SEO配置失败，请稍后重试”，运营无法新增或维护页面 SEO 条目 - 网站内容 / SEO 管理 - 需排查 SEO 保存链路与写入校验。


### 🟡 中优先级
- [ ] [运营] 积分记录接口当前仍返回 `points` 有值但 `amount=0`、`balance=null`、`reason` 为空；前端列表会把塔罗消耗等非 `reduce` 类型记录误判成“增加”，运营无法准确完成积分审计 - 积分管理 / 积分记录 - 需统一方向、数量、余额与原因字段映射，避免正负向记录继续失真。


### 🟡 中优先级（体验问题）
- [ ] [UI] 合婚历史记录点击后会直接覆盖当前正在填写的双方信息，没有任何二次确认 - 合婚页 / 历史记录列表 - 建议在检测到未提交草稿时弹出确认框，支持“继续填写”或“载入历史记录”。

- [ ] [UI] 每日运势“详细运势”默认只展开 `career` / `wealth`，当天若只返回感情或健康分项，首屏会全部折叠 - 每日运势页 / 详细运势折叠面板 - 建议在数据加载后按接口实际返回的首个有效分项自动展开，保证内容首屏可见。
- [ ] [UI] 八字流年分析的年份选择条在小屏下标签、滑块和年份值共占一行，可拖动区域过窄，影响年份精确选择 - 八字排盘页 / 流年运势深度分析 / 年份选择 - 建议移动端改为上下布局，或缩短标签并给当前年份值单独换行。

### 🟢 低优先级（美观问题）
- [ ] [UI] 每日运势综合分与星级换算不一致，85-89 分仍只显示四星，视觉评价比数字分偏低 - 每日运势页 / 今日综合运势卡 - 建议统一分数与星级映射规则，避免同一张卡片出现“双重口径”。

## 2026-03-18 UI设计检查（第四十六次）


### 🟡 中优先级（体验问题）


- [ ] [UI] 塔罗页问题模板区在用户一输入内容后就整体隐藏，无法继续对比或切换模板，灵感输入流程被打断 - 塔罗占卜页 / 问题引导区 - 建议改为可折叠面板，或在输入框下保留“继续查看模板”入口。
- [ ] [UI] 六爻结果页没有回显起卦方式与起卦时间上下文，用户回看当前结果或历史记录时很难确认“这卦到底怎么起的” - 六爻占卜页 / 结果卡 - 建议把 `method_label` 与 `time_info` 展示到结果头部或问题区。
- [ ] [UI] 每日运势“专属运势”卡首屏直接展示“比劫 / 印绶 / 官杀 / 财星”等术语，普通用户很难立刻理解这是利好还是提醒 - 每日运势页 / 专属运势卡 - 建议补充白话副标题、tooltip 映射或“这对你意味着什么”的简述。

### 🟢 低优先级（美观问题）

## 2026-03-18 占卜深度体验补充（第四十七次）

### 🔴 高优先级（逻辑错误/准确性问题）
- [ ] [占卜] 每日运势在登录且已有八字记录后仍返回 `personalized: null`，专属运势卡实际永远不出现 - 每日运势 / `GET /api/daily/fortune` - 2026-03-18 用测试账号 `13800138112` 复测：样例八字排盘成功后 `points/balance` 已显示 `baziCount=2`、余额从 100 变 90，但日运接口仍只返回公共黄历与通用分项，登录用户与游客看到的是同一份结果。
- [ ] [占卜] 六爻时间起卦与手动摇卦在当前运行态都返回业务码 500，历史记录始终为空，主链路仍无法真正起卦 - 六爻占卜 / `POST /api/liuyao/divination` + `GET /api/liuyao/history` - 2026-03-18 用 JSON 请求复测：`pricing` 正常返回免费，但 `time` 与 `manual` 两种方式均为 `HTTP 200 + code 500`，随后历史接口仍是空列表。
- [ ] [占卜] 塔罗三种牌阵都能正常抽牌并扣分，但解读接口统一 500，用户只能看到牌面拿不到任何正式结论 - 塔罗占卜 / `POST /api/tarot/interpret` - 2026-03-18 对 `single / three / celtic` 连续复测时，`draw` 都成功且各扣 5 分，`interpret` 全部返回 HTTP 500；容器错误日志明确命中 `Class "app\\controller\\TarotElementService" not found`。
- [ ] [占卜] 合婚 premium 详细报告仍给出 `81 分 / 佳偶天成`，但九宫命卦同时判为“五鬼 / 大凶 / 强烈建议慎重考虑”，总评与传统断语继续相互冲突 - 合婚配对 / premium 详细报告 - 2026-03-18 使用新测试账号 `13800138113` 真实解锁后仍复现，用户会被高分总评掩盖传统凶配风险。

### 🟡 中优先级（体验问题）






## 2026-03-18 数据库表优化与功能完善（第四十八次）

### 🔴 高优先级（核心架构优化）

#### 数据库表优化
- [ ] [架构] 删除或合并冗余的用户资料表 `tc_user_profile` - 该表字段完全可以合并到 `users` 主表中，当前两表冗余且需要 JOIN 查询，影响性能 - 建议：直接在 `users` 表添加 `gender/age/region/education/occupation` 等字段，然后删除 `tc_user_profile`
- [ ] [架构] 合并冗余的积分任务表 `tc_points_task` 与 `tc_task_log` - 两个表记录相同内容，容易造成数据不一致 - 建议：统一使用 `tc_task_log`，删除 `tc_points_task`，在迁移脚本中合并历史数据
- [ ] [架构] 检查并整合两个签到表 `checkin_record` 与 `tc_checkin_log` - 功能重复，维护困难 - 建议：确认业务逻辑后选择一个作为主表，另一个作为历史备表或删除

#### 敏感信息加密
- [ ] [安全] 对 `tc_payment_config.api_key` 和 `tc_payment_config.api_cert` 实现字段级加密存储 - 当前直接存储明文，存在安全风险 - 建议：使用 Laravel 的 Encryptable 或自定义加密方法
- [ ] [安全] 对 `tc_payment_config.mch_id` 实现加密存储 - 商户号属于敏感信息
- [ ] [安全] 对 `tc_sms_config.secret_id` 和 `tc_sms_config.secret_key` 实现加密存储 - 短信服务凭据必须加密
- [ ] [安全] 对 `tc_sms_config.sign_name` 加密存储 - 短信签名也是敏感标识
- [ ] [安全] 考虑对 `users.phone` 实现加密存储或脱敏处理 - 用户手机号属于 PII（个人可识别信息）

### 🟡 中优先级（功能完善）

#### 未实现的功能表
- [ ] [功能] 激活流年运势功能 - `tc_yearly_fortune` 表已创建但代码中未使用 - 建议：
  - 在前端添加"流年运势"功能入口
  - 创建流年运势计算逻辑
  - 接入 AI 进行深度分析
  - 参考表字段：user_id, year, overall_score, career, wealth, love, health, analysis
  
- [ ] [功能] 激活取名功能 - `tc_qiming_record` 表已创建但代码中未使用 - 建议：
  - 在前端添加"取名建议"功能入口
  - 创建取名建议生成逻辑（基于八字五行）
  - 集成 AI 进行名字质量评测
  - 参考表字段：user_id, bazi_data, suggested_names, rating_scores, analysis

#### 表字段完善
- [ ] [数据] 在 `users` 表补充验证码相关字段 - 当前必须查 `tc_sms_code` 表才能验证状态 - 建议：添加 `last_sms_code_time`, `sms_code_attempts` 字段以优化查询
- [ ] [数据] 在 `points_records` 表补充更多业务分类 - 当前 `action` 字段值不统一 - 建议：建立 action 的枚举定义，并补充缺失的业务场景分类
- [ ] [数据] 在 `bazi_records` 表添加 `ai_analysis_model` 字段 - 记录使用的 AI 模型（GPT-3.5/GPT-4/Claude等），便于追溯和对比结果

### 🟢 低优先级（性能与运维）

#### 性能优化与监控
- [ ] [性能] 为 `points_records` 表建立按用户 ID 的分区索引 - 该表是高频写表，Year 2 预计 ~1M 行 - 建议：`KEY idx_user_created (user_id, created_at)`
- [ ] [性能] 为 `operation_logs` 表建立定期清理策略 - 该表增长快，Year 2 预计 ~100MB - 建议：仅保留最近1年的操作日志，老数据定期归档到文件系统或备份库
- [ ] [性能] 为 `tc_checkin_log` 表建立分区表结构 - 签到数据量大且增长快 - 建议：按日期分区，方便定期清理或归档
- [ ] [性能] 引入 Redis 缓存策略 - 避免重复查询配置表、模板表等 - 建议：
  - 缓存 `system_config` 全表（1小时过期）
  - 缓存 `ai_prompts`（按 category 分组）
  - 缓存 `tarot_cards` 和 `tarot_spreads`
  - 缓存用户的 VIP 状态（30分钟过期）
  
- [ ] [性能] 对高频查询表建立必要的复合索引 - 建议：
  - `bazi_records`: `KEY idx_user_created (user_id, created_at)`
  - `tc_tarot_record`: `KEY idx_user_spread_created (user_id, spread_type, created_at)`
  - `recharge_orders`: `KEY idx_user_status_created (user_id, status, created_at)`


#### 数据归档与清理
- [ ] [运维] 为 `tc_sms_code` 表建立过期验证码清理 - 定期删除 `expire_time` 已过期的记录

## 2026-03-18 UI设计检查（第四十九次）

### 🔴 高优先级（功能性问题）



### 🟡 中优先级（体验问题）

- [ ] [UI] 塔罗分享文案默认把“问题全文 + 全部抽牌结果”直接写入系统分享 / 剪贴板文本；即使用户只是想发链接，也会一次性暴露较私密的提问内容和牌面细节 - 塔罗占卜页 / 分享流程 - 建议默认只分享摘要与链接，并把“附带问题与牌面详情”改成显式二次确认。



### 🟢 低优先级（美观问题）


## 2026-03-18 UI设计检查（第五十次）

### 🔴 高优先级（功能性问题）


### 🟡 中优先级（体验问题）

- [ ] [UI] 合婚结果页只有“重新测算”会调用 `resetForm()` 全量清空双方信息；用户想回去改一个出生时间或补充姓名时，没有“返回修改并保留当前输入”的路径，返工成本过高 - 合婚页 / 详细报告结果区 - 建议增加“返回修改”入口，仅收起结果并保留当前表单值，和“重新测算（清空）”分开。

- [ ] [UI] 每日运势页把“未登录游客 / 已登录但未排盘 / 个性化字段异常或缺失”统一折叠成同一张“去排盘”提示卡；用户无法判断自己是没登录、没建档，还是系统没正确返回个性化数据 - 每日运势页 / 个性化运势区域 - 建议拆分访客态、未排盘态、个性化加载失败态三种提示，并分别提供登录、排盘或重试入口。





