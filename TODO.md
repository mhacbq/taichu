## 待处理项目

### 🔴 高优先级
- [x] [运营] 2026-03-18 回归复测确认：用户列表首屏虽能正常加载，但按“用户名=用户8001”搜索后仍会立刻进入“用户列表加载失败”只读态；同时用户详情页“用户名”字段仍展示手机号 `13800138001`，运营无法稳定按用户名检索并核对用户身份 - 用户管理 / 用户列表搜索 / 用户详情 - 已统一用户列表关键词检索的 `whereOrLike` 兼容写法，并收口用户名展示口径，避免手机号继续回填到“用户名”字段。
- [x] [运营] 用户详情页“手动调积分”弹窗可打开，但提交最小化 `+1` 积分后连续提示“调整积分失败，请稍后重试”；对应 `POST /api/admin/points/adjust` 的 HTTP 请求已成功返回 200，导致人工补偿/扣减积分无法落地 - 用户管理 / 用户详情 / 积分调整 - 已补齐列表页积分调整权限保护，并新增 `database/20260318_fix_admin_role_permissions.sql` 给运营角色补发 `points_adjust` 权限，避免接口返回业务 403 但网络层仍显示 200 的误导状态。
- [x] [运营] 2026-03-18 回归复测确认：黄历管理 `/content/almanac`、SEO 管理 `/site/seo` 与 VIP 订单 `/payment/vip-orders` 当前在真实登录后仍会直接进入“加载失败”只读态，运营依然无法维护黄历、调整 SEO 或处理会员订单 - 内容管理 / SEO / VIP订单 - 已补齐后台角色权限补丁的自动执行链路，并为 VIP 订单页补充页内权限定位信息，确保运营真实登录态拿到黄历维护所需权限，SEO/VIP 页面也可更快定位失败来源。


### 🟡 中优先级
- [x] [运营] 知识库分类列表中文名称显示为 `å…«å—åŸºç¡€` 一类乱码，文章侧虽然能进入，但运营无法辨认真实分类并准确归档文章 - 网站内容 / 知识库文章 / 分类管理 - 已修正后台知识库分类树/分类路径的乱码修复链路，并把分类名称归一化覆盖到列表、下拉与文章详情路径，恢复中文可读性。
- [x] [运营] 积分记录页按用户 `3` 过滤后，`2026-03-18 01:30:20` 的“连续签到1天”正向记录仍被展示成“减少”，且“变动数量 / 变动后余额 / 变动原因”三列大量为空或仅显示 `-`，运营无法判断积分到底是增加还是扣减，更无法完成审计追踪 - 积分管理 / 积分记录 - 已打通后台积分记录兼容映射，统一增减方向、记录类型、数量、余额与原因字段，签到/充值等正向记录不再被误判为扣减。



### 🟢 低优先级
- [x] [运营] Dashboard 当前只展示用户量、活跃与八字/塔罗使用量，未直接呈现订单数、收入等经营指标卡片，运营还得再切到订单页补数据 - 后台首页 / Dashboard - 已接入今日/本月支付订单与实收金额卡片，并补齐月度已支付订单统计口径，运营可直接在看板查看核心经营指标。

## 2026-03-18 UI设计检查（第四十五次）

### 🔴 高优先级（功能性问题）
- [ ] [UI] 塔罗点击“分享”会先把记录设为公开，用户即使取消系统分享也不会回滚隐私状态 - 塔罗占卜页 / 结果区 / 分享按钮 - 建议先补一个“是否公开分享”确认步骤，再在用户明确同意后调用公开接口；取消分享时应保持私密状态。
- [ ] [UI] 六爻主提交按钮引用了未定义的 `submitButtonText`，运行时可能出现空白 CTA 或直接报错 - 六爻占卜页 / 起卦表单 / 主提交按钮 - 建议补齐按钮文案计算逻辑，按首次免费、VIP免费、普通扣分等状态返回明确文案。
- [ ] [UI] 合婚在切换“精确时分 / 大概时段 / 未知时辰”时共用同一输入值，可能出现输入框看似已空但旧值仍被提交 - 合婚页 / 双方出生信息表单 - 建议在精度切换时同步归一化并清理不兼容值，或拆分日期与时间字段，避免展示值和提交值脱节。

### 🟡 中优先级（体验问题）
- [ ] [UI] 首页“更多功能”入口承诺的是功能集合，实际却跳转到个人中心，首次访问很容易误以为点错页面 - 首页 / 我们的服务 / 更多功能卡片 - 建议改成真实的功能聚合页，或把入口文案改成“个人中心 / 更多记录与权益”。
- [ ] [UI] 合婚历史记录点击后会直接覆盖当前正在填写的双方信息，没有任何二次确认 - 合婚页 / 历史记录列表 - 建议在检测到未提交草稿时弹出确认框，支持“继续填写”或“载入历史记录”。
- [ ] [UI] 每日运势“详细运势”默认只展开 `career` / `wealth`，当天若只返回感情或健康分项，首屏会全部折叠 - 每日运势页 / 详细运势折叠面板 - 建议在数据加载后按接口实际返回的首个有效分项自动展开，保证内容首屏可见。
- [ ] [UI] 八字流年分析的年份选择条在小屏下标签、滑块和年份值共占一行，可拖动区域过窄，影响年份精确选择 - 八字排盘页 / 流年运势深度分析 / 年份选择 - 建议移动端改为上下布局，或缩短标签并给当前年份值单独换行。

### 🟢 低优先级（美观问题）
- [ ] [UI] 每日运势综合分与星级换算不一致，85-89 分仍只显示四星，视觉评价比数字分偏低 - 每日运势页 / 今日综合运势卡 - 建议统一分数与星级映射规则，避免同一张卡片出现“双重口径”。




